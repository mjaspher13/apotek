import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
      flex: 1,
      flexDirection: 'row',
      width: 1,
    },
    tabContainer: {
      flex: 1,
      flexDirection: 'column',
      alignItems: 'center',
    },
    image: {
      width: 1,
      height: 1,
      // flex: 1,
    },
    label: {
      textAlign: 'center'
    }
});
